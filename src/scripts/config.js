// Key used to store the access token in localStorage
export const ACCESS_TOKEN_KEY = 'accessToken';

// Base URL for the API endpoints
export const BASE_URL = 'https://story-api.dicoding.dev/v1';

// API key for the map service (if applicable)
export const MAP_SERVICE_API_KEY = 'G297u1oZuDeSnvZdNec1';

